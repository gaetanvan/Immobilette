{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block content %}
    <hr>
    <div class="row text-center mt-5">
        <form class="col mx-5" method="post">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            {% if app.user %}
                <div class="mb-3">
                    You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                </div>
            {% endif %}

            <h1 class="h3 mb-3 darkblue fw-600">Connexion</h1>
            <label class="darkblue fw-600" for="inputEmail">Mail</label>
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control mb-3" autocomplete="email" required autofocus>
            <label class="darkblue fw-600" for="inputPassword">Mot de passe</label>
            <input type="password" name="password" id="inputPassword" class="form-control mb-3" autocomplete="current-password" required>

            <input type="hidden" name="_csrf_token"
                   value="{{ csrf_token('authenticate') }}"
            >

            {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html

                <div class="checkbox mb-3">
                    <label>
                        <input type="checkbox" name="_remember_me"> Remember me
                    </label>
                </div>
            #}
            <button class="backgroundDarkblue btnRadius btn text-white btn-lg btn-sm mt-3 px-3" type="submit">
                Connexion
            </button>
            <a class="d-block smallFont" href="{{ path('app_register') }}">Inscription</a>
        </form>
    </div>
{% endblock %}
